<!DOCTYPE html>
<html>
<head>
  <title>Salary Slip - <%= employee.name %></title>
  <style>
    .salary-slip {
      max-width: 800px;
      margin: 30px auto;
      background: white;
      padding: 40px;
      border: 2px solid #333;
    }
    .slip-header {
      text-align: center;
      border-bottom: 2px solid #333;
      padding-bottom: 20px;
      margin-bottom: 30px;
    }
    .slip-row {
      display: flex;
      justify-content: space-between;
      margin: 15px 0;
      padding: 10px 0;
      border-bottom: 1px solid #ddd;
    }
    .slip-row.total {
      font-weight: bold;
      font-size: 18px;
      border-top: 2px solid #333;
      border-bottom: 2px solid #333;
      margin-top: 20px;
    }
    .earnings, .deductions {
      margin-top: 30px;
    }
    .section-title {
      font-weight: bold;
      font-size: 18px;
      color: #333;
      margin-bottom: 15px;
    }
    @media print {
      .no-print {
        display: none;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="no-print">
      <a href="/employees" class="btn">Back to Employee List</a>
      <button onclick="window.print()" class="btn" style="background: #28a745;">Print Salary Slip</button>
    </div>
    
    <div class="salary-slip">
      <div class="slip-header">
        <h1>SALARY SLIP</h1>
        <p>Month: <%= new Date().toLocaleDateString('en-IN', { month: 'long', year: 'numeric' }) %></p>
      </div>
      
      <div class="slip-row">
        <span><strong>Employee Name:</strong></span>
        <span><%= employee.name %></span>
      </div>
      
      <div class="slip-row">
        <span><strong>Position:</strong></span>
        <span><%= employee.position %></span>
      </div>
      
      <div class="slip-row">
        <span><strong>Department:</strong></span>
        <span><%= employee.department.name %></span>
      </div>
      
      <div class="slip-row">
        <span><strong>Email:</strong></span>
        <span><%= employee.email %></span>
      </div>
      
      <div class="slip-row">
        <span><strong>Date of Joining:</strong></span>
        <span><%= new Date(employee.hireDate).toLocaleDateString() %></span>
      </div>
      
      <div class="earnings">
        <div class="section-title">EARNINGS</div>
        
        <div class="slip-row">
          <span>Basic Salary</span>
          <span>Rs <%= employee.salary.basicSalary.toLocaleString() %></span>
        </div>
        
        <div class="slip-row">
          <span>HRA (House Rent Allowance)</span>
          <span>Rs <%= employee.salary.hra.toLocaleString() %></span>
        </div>
        
        <div class="slip-row">
          <span>DA (Dearness Allowance)</span>
          <span>Rs <%= employee.salary.da.toLocaleString() %></span>
        </div>
        
        <div class="slip-row">
          <span>Bonus</span>
          <span>Rs <%= employee.salary.bonus.toLocaleString() %></span>
        </div>
        
        <div class="slip-row total">
          <span>GROSS SALARY</span>
          <span>Rs <%= employee.salary.grossSalary.toLocaleString() %></span>
        </div>
      </div>
      
      <div class="deductions">
        <div class="section-title">DEDUCTIONS</div>
        
        <div class="slip-row">
          <span>PF (Provident Fund)</span>
          <span>Rs <%= employee.salary.pf.toLocaleString() %></span>
        </div>
        
        <div class="slip-row">
          <span>Tax</span>
          <span>Rs <%= employee.salary.tax.toLocaleString() %></span>
        </div>
        
        <div class="slip-row total">
          <span>TOTAL DEDUCTIONS</span>
          <span>Rs <%= employee.salary.totalDeductions.toLocaleString() %></span>
        </div>
      </div>
      
      <div class="slip-row total" style="background: #f0f0f0; padding: 15px; margin-top: 30px; font-size: 20px;">
        <span>NET SALARY</span>
        <span>Rs <%= employee.salary.netSalary.toLocaleString() %></span>
      </div>
      
      <p style="margin-top: 40px; font-size: 12px; color: #666;">
        This is a computer-generated salary slip and does not require a signature.
      </p>
    </div>
  </div>
</body>
</html>