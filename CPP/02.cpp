// learning if-else
#include <iostream>
using namespace std;

int main(){
    int a;
    cin>>a;
    int b = 3;

    // if-else
    // else-if
    // nestate

    if ( (a >= 20 && a <= 40) && (a%2 == 0 && a%3 == 0)  )
    {
       cout<<a;
    }
    else{
        cout<<"not divisible";
    }
    
    cout<<"something";
}